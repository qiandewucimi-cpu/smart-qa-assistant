# 智能问答助手 · 项目复盘报告

> 阶段 7 产出（对应 `docs/项目计划书.md` §四 阶段 7）
> 版本 **v1.2** ｜ 更新：2026-09-12 ｜ 状态：**已完成，除「用户价值实测量」待上线后回填**
>
> **v1.2 更正（重要）**：v1.1 曾把 C3/O4 的修复归因于「**开启向量检索**」，**该归因错误**。
> 复核发现：**向量检索全程未生效**——配置开了、764/817 页索引也建成了，但查询期 embedding
> 调用因**账户额度耗尽**（智谱 code 1113）而 429，应用静默降级；评测中出现 **0 个向量召回事件**、
> `/search` 的 `vectorHits` 恒为 0。真实归因是 **① 检索深度 topK 5→15；② 问答模型换 `glm-4.5-air`**
> （单变量对照见 `eval/评测报告_v1.0_检索深度与快模型.md` §7）。
>
> **v1.1 更新（两项 P0 落地）**：① 检索深度 topK 提高 → 偶发零召回归零；
> ② **问答模型换 `glm-4.5-air`** → 端到端延迟 P50 **61.5s → 18.0s（↓3.4×）**。
> 命中率 **85.7% → 90.5%**、引用可溯源 **90.9% → 100%**、幻觉仍 **0/21**。
> 详见 `eval/评测报告_v1.0_检索深度与快模型.md`。
>
> 关联文档：`docs/质量门槛对标.md`（质量维度对标）、`docs/简历素材.md`（简历与面试话术）、
> `eval/评测报告_v0.3_复测.md`、`eval/评测报告_v1.0_检索深度与快模型.md`、`eval/拒答评测报告.md`、`docs/项目进度表.md`
>
> **本报告所有数字均可复算**，逐条来源见 §9。凡不能复算的，一律标注「待实测」而不给估计值。

---

## 0. 一句话总结

把散落在聊天记录、会议纪要、培训手册与界面截图中的 **173 份业务文档**，
经零依赖清洗与脱敏管线（**4,667 处命中、零残留**）编译为 **819 页可溯源结构化 Wiki**，
再通过飞书机器人提供问答服务。22 题评测**命中率 90.5%（19/21）、幻觉 0/21、
引用可溯源 100%、拒答 8/8**，达到计划书的准确率与忠实度目标；
**唯一未达标项是延迟（P50 18.0s vs 目标 <3s）**——已从「分钟级」压到「十几秒级」（↓3.4×），
但仍是架构性 trade-off，已如实标注并给出优化路径。

---

## 1. 目标 vs 结果（逐条对照计划书 §二）

| # | 指标 | 计划目标 | 实测结果 | 判定 | 证据 |
|---|---|---|---|---|---|
| 1 | 数据覆盖 | 待盘点 | 源 **173 份 / 153MB** → 清洗 **121 份 / 105.2 万字符** → 入库 **119/119（100%）** | ✅ | `数据盘点表.md`、`corpus_stats.py`、`wiki_status.py` |
| 2 | 检索命中率 | ≥ 85% | **90.5%**（19/21 计分题；95% Wilson 区间 71.1~97.3%） | ✅ **达标** | `评测报告_v1.0_检索深度与快模型.md` |
| 3 | 答案准确率 | ≥ 80% | **90.5%**（同上口径） | ✅ **达标** | 同上 |
| 4 | 幻觉率 | ≤ 5% | **0/21**（95% 置信上界 **13.3%**） | ✅ 达成 | 同上 |
| 5 | 检索效率 | < 3s | **P50 18.0s / 均值 22.1s**（较上轮 61.5s **↓3.4×**） | ❌ **未达标**（但大幅改善） | `评测结果_raw.json` 复算 |
| 6 | 节省时长 | 待测量 | 埋点已就绪，**真实数据待上线后回填**（见 §6） | ⏳ 待实测 | `usage_stats.py` |

**达成 4 / 6，1 项未达标（延迟，已大幅收窄），1 项待实测。**

> 口径说明（面试必答）：第 4 项「幻觉率 ≤5%」的目标**在设计上就已达成**——系统强制答案挂来源页、
> 检索不到则拒答，「编造」在设计阶段即被阻断，因此 0 幻觉是**架构副产品**而非调优结果。
> 但**统计上不能写成 0%**：21 题全零只能负责任地说「真实幻觉率 < 13.3%」（要支撑「<5%」需 ≥60 题）。


---

## 2. 交付物清单

| 类型 | 交付物 | 规模 |
|---|---|---|
| **代码** | `scripts/` **33 个公开 Python 脚本** | **5,683 行**，主链路零第三方依赖（仅 PDF 与飞书 SDK 引外部包） |
| **数据** | `data/clean/` 清洗脱敏语料 | 121 份 / 105.2 万字符 |
| **知识库** | LLM Wiki 编译产物 | 819 页（见 §3.1）；另有 764 页已建向量索引（**当前未生效**，见 §4.7） |
| **评测** | `eval/` 评测集与报告 | 22 题主评测 + 13 题拒答专项 + 稳定性复跑 + 延迟基准，共 **18 个文件**（4 份报告 md + 6 份原始结果 json + 日志等） |
| **文档** | `docs/` 项目文档 | 11 份 / 约 7 万字符 |
| **应用** | 飞书机器人 `feishu_bot.py` | 长连接 + 卡片 + 多轮 + 埋点 |

---

## 3. 量化成果

### 3.1 数据与知识库

| 环节 | 指标 | 数值 |
|---|---|---|
| 源数据 | 文件数 / 体积 | 173 份 / 153MB |
| 清洗 | 成品语料 | 121 份 / **1,052,184 字符**（105.2 万） |
| 清洗 | 截图 OCR | **633 张**界面截图 → 43 份 OCR 产物（含 DOCX 内嵌图，按页聚合） |
| 脱敏 | 命中总数 | **4,667 处**（文本 1,586 + 截图 OCR 3,081） |
| 脱敏（输入侧） | 残留 | **0**（`data/clean/` 121 份；独立巡检脚本 + **词边界**匹配） |
| 脱敏（输出侧） | 残留 | **0**（提交前跑 `sanitize_eval_results.py --check`；见 §5 坑 10） |
| 脱敏（wiki 派生） | 残留 | **58 处 / 11 文件**（仅存在于 **gitignored** 的 `projects/`；公开仓库不受影响）——详见下 |
| 入库 | 编译完成率 | **119 / 119（100%）**，失败 0 |
| 知识库 | Wiki 页数 | **819 页** |

**Wiki 结构（819 页）**：概念 353 ｜ 实体 243 ｜ 来源摘要 119 ｜ 发现 74 ｜ 高频查询 14 ｜ 方法论 11 ｜ 综合 3 ｜ 索引/日志 2

> **⚠️ 一处必须说准的地方（「零残留」的范围）**：`audit_leaks.py` 对两处目标的结论**不同**——
> **`data/clean/`（输入侧）121 份命中 0 处**（干净，这是脱敏闸门的成果）；
> 但 **`projects/`（wiki 编译产物）命中 58 处 / 11 文件**。这 58 处属三类"派生"：
> ① **真实订单号**（实体页名形如 `采购订单-<订单号>` / `订单号-<订单号>`）；
> ② **海外地名**——wiki 页把工厂"所在国家"写成某个**南亚国家名**，并标了"（根据银行地址推断）"；
> ③ 被 app 的 Review 引用的**客户名**（在 `.llm-wiki/review.json`）。
>
> 关键是：**`data/clean` 是干净的，这些词是 LLM 编译时"推断"出来的**——
> 输入里只有被脱敏的 `[地址]`，模型凭银行地址里的城市线索，用**世界知识**把国家名补了出来。
> **这类残留任何词表都挡不住**（词表只能挡"输入里有的词"），只能靠入库后巡检发现 + 人工判断是否回改。
> **影响面**：`projects/` 已被 `.gitignore` 隔离 → **公开仓库、简历、面试材料均不含这些内容**；
> 但**取用 wiki 内容写对外文档时要人工过一遍**（本项目就因此把一处供应商页名从评测报告里去掉，见 §5 坑 10）。
>
> > 🔒 **本报告的写法示范**：以上三类**全部用描述性说法**（"某个南亚国家名""客户名""<订单号>"），
> > **没有写出任何一个真实词**——因为这份报告是入库文件。这是本项目的硬规矩：**解释脱敏规则时也不能举真实例子。**


### 3.2 质量指标（含口径，缺一不可）

| 维度 | 结果 | 口径 / 置信界 |
|---|---|---|
| 命中率 | **90.5%**（19/21） | 21 道计分题；P2 因语料口径矛盾标不计分；95% Wilson 区间 71.1~97.3% |
| 幻觉 | **0/21** | 95% 置信**上界 13.3%**（`1-0.05^(1/21)`） |
| 拒答准确性 | 语料外 **8/8**、0 编造 | 95% 置信**下界 68.8%**（`0.05^(1/8)`）；语料内对照 **5/5** 无误拒 |
| 引用可溯源 | **100%**（22/22） | 全部题目均带来源引用（上轮 90.9%，本轮 2 题零召回已修复） |
| 语料覆盖度 | **100%**（119/119） | 未入库 23 份均为有意排除的客户原始订单 PDF 与重复件 |
| 向量索引覆盖 | **93.5%**（764/817）**但当前未生效** | 53 页未建成（25 词条同名 stem 冲突 + 2 页名称含撇号）；且查询期 embedding 因**额度耗尽**失败 → 检索实际仍是「关键词+图谱」两路 |
| 延迟 | P50 **18.0s** / 均值 22.1s / P90 31.6s | 端到端（含查询改写 → 混合检索 → 长答案生成）；**未达标**，较上轮 **↓3.4×** |
| 稳定性 | P1 稳定未命中（0/3）｜D2 偶发失败（2/3） | 同题复跑 3 次（`评测结果_raw_stab_D2P1.json`） |

**分项最佳**：报价配比 **6/6（100%）**、对比 2/3、机制 1/1。

> **⚠️ 「幻觉 0/21」必须加一句限定（本轮新发现）**：P1 那道题的失败模式**不稳定**——
> 多次采样里多数是「拒答」，但**有一次给出了未被语料支持的流程描述**
> （自行补出"数据提取、转换和映射"这类语料中不存在的措辞）。
> 因此「0/21」准确的含义是「**本轮 21 次采样中 0 次无依据编造**」，
> **不等于"任何提问下都不会编造"**。P1 应按「答偏 + 轻度编造倾向」记账，不能算作安全拒答。

### 3.3 四轮评测演进（口径可比性已对齐）

| 轮次 | 语料覆盖 | 检索 / 问答模型 | 题数 | 命中率 | 幻觉 | 关键动作 |
|---|---|---|---|---|---|---|
| 基线 09-10 | 12 份（19%） | 关键词+图谱 / glm-4.6 | 15 | 73.3% | 0/15 | 首版评测，缺口定位为语料未覆盖 |
| v0.2 09-12 | **119 份（100%）** | 关键词+图谱 / glm-4.7 | 22 | 81.0% | 0/21 | 语料扩容 10×；O3/O5 因网关 502 丢分 |
| v0.3 09-12 | 119 份 | 关键词+图谱 / glm-4.7 | 22 | 85.7% | 0/21 | **修评测链路工程缺陷**，3 题转命中 |
| **v1.0 09-12** | 119 份 | 关键词+图谱（**向量未生效**）/ glm-4.5-air | 22 | **90.5%** | **0/21** | **提高检索深度 topK 5→15 + 换快模型** |
| **v1.1 09-12** | 119 份 + **1 页人工沉淀** | 同上 | 22 | **85.7%**（两轮均 18/21） | 0/21 | **P1 知识固化 + 稳定性采样** |

> 口径提醒：v1.0 虽然**配置并建好了**向量索引，但查询期 embedding 因额度耗尽失败，
> 检索实际仍走「关键词 + 图谱」两路——**本行不写作「混合检索」**，避免高估。
> C3/O4 的修复来自检索深度与模型的叠加，与向量无关（详见 `eval/评测报告_v1.0_检索深度与快模型.md` §7）。

> ⚠️ **v1.1 的重要修正：v1.0 的 90.5% 不是可复现指标。**
> v1.1 用**同一套配置**连跑两轮，都是 **18/21（85.7%）**，且**两轮未命中的题目几乎不重合**
> （r1 = D1/D2/P1，r2 = D1/P3/O2，交集只有 D1）。
> 也就是说：**命中率的方差主要来自框架的随机失败，而不是「哪些知识还没有」**。
> 因此对外一律报「**18~19/21，随框架随机失败波动 ±1~2 题**」，不报单一数字。
> 详见 `eval/评测报告_v1.1_知识固化与稳定性.md` §5–§7。


---

## 4. 关键决策复盘（哪些判断是对的）

### 4.1 把脱敏做成「入库前唯一闸门 + 入库后巡检」

实测发现 LLM 编译会把客户名**扩散成独立实体页与索引**，事后清理需改动 24+ 个 wiki 页并重建索引。
因此定为「先脱敏再入库」，再用 `audit_leaks.py` 入库后反查兜底（双保险）。
**代价**：词表是打地鼠，必须持续维护；**收益**：公开仓库与简历零合规风险。

### 4.2 命中率不达标时，先怀疑工具链而不是模型（本轮最值钱的一条）

v0.2 的 81.0% 里有 **2 分是被评测脚本自身吃掉的**：

| 缺陷 | 症状（易误判） | 修复 |
|---|---|---|
| 脚本走 `/projects/current/chat`，`currentProject` 为 null 时该端点 **404** | 全题失败，**看起来像网关挂了** | 新增 `resolve_project_id()`，从 `/api/v1/projects` 取 ID |
| 网关 502 抖动**无重试**，直接判失败 | 偶发、不可复现的"模型答不出" | 对 429/5xx/超时加指数退避重试（3 次） |
| 顶层 `references` 有时为空，无法区分「检索到未采用」与「真零召回」 | 把**检索抖动**误判成**语料缺口** | 从 `events` 兜底提取引用 |

修掉后 O3、O5、C1 三题转命中，**+4.7pp**。

### 4.3 瓶颈转移判断：从「语料覆盖」到「检索稳定性与排序」

三轮归因后发现：剩余 3 道未命中里 **0 道是语料缺失**——2 道是检索偶发零召回（内容在库、上轮还命中过），
1 道是检索排序。**据此停止扩充语料**，把精力转向检索层（提高检索深度、换问答模型），避免无效的数据堆砌。
这个「判断的转移」本身就是结论。

### 4.4 分模型路由（入库用快、问答用强）

自建 `bench_models.py` 实测定位编译瓶颈：glm-4.7 单次 **66.6s**（九成输出是思维链）
vs glm-4.5-air **7.1s**（**快 9.4×**）。落地「入库 `glm-4.5-air`、问答 `glm-4.7`、
截图 OCR `glm-4v-flash`」的分路由，全量编译耗时从数十小时降到可接受范围。

### 4.5 指标一律带样本量与置信界（避免被自己的数字击穿）

放弃「幻觉率 0%」这类表述，改为「**0/21（95% 置信上界 13.3%）**」。
0 事件的 Clopper–Pearson 单侧上界 `1-α^(1/n)` 让口径可被追问、可被验证——**敢认局限比数字漂亮更可信**。

### 4.6 主动补测「拒答准确性」，补齐零幻觉的评估盲区

22 题主评测的「幻觉 0」有**结构性盲区**：那 21 题的答案语料里全都有，模型只要正常检索就能答对，
**没机会暴露「没有依据时会不会硬编」**。故补 13 题专项（**一正一反，缺一不可**）：

| 组别 | 题数 | 期望行为 | 实测 |
|---|---|---|---|
| X 类·语料外（含 2 道「相邻概念诱导」题） | 8 | 拒答 | **8/8 正确拒答、0 编造** |
| N 类·语料内冷门对照（22 题未问过的模块） | 5 | 作答 | **5/5 正常作答、0 误拒** |

只测 X 类是不够的——若模型「检索不到就一律拒答」，光看 X 类会得出「拒答能力很强」的**错误结论**；
加对照组才能区分**真拒答** vs **摆烂式拒答**。

> **最有价值的发现：模型是「内容驱动」而非「检索驱动」。** X 类 8 题里有 7 题检索都返回了 5~15 条内容，
> 模型仍正确拒答。这**避开了 RAG 最常见的失败模式——检索恒有返回 → 模型强行凑答案**。

### 4.7 两项 P0 的落地、一次归因纠错，与「副作用记账」

阶段 6/7 遗留的两项 P0（检索零召回、延迟）在本轮一起做掉。
**更重要的是：本轮做了唯一一次归因纠错——初版把 C3/O4 的修复记在「开向量检索」头上，复核证明那是错的。**

| P0 | 做法 | 真实收益 | **新生的副作用** |
|---|---|---|---|
| ① 检索零召回（C3/O4） | 检索深度 **topK 5 → 15** | C3/O4 零召回 → **命中**；引用可溯源 90.9% → **100%** | ⓐ 检索面变宽，**P1 由「答偏」退化为「拒答」**（被稀释）；ⓑ 产物里开始出现**编译期派生的真实实体页**（见 §5 坑 10） |
| ② 延迟 | 问答 `glm-4.7` → `glm-4.5-air` | P50 **61.5s → 18.0s**（↓3.4×），命中率不降反升 | D2 触发框架 8 步迭代上限（偶发，复跑 2/3 命中） |
| ~~③ 开向量检索~~ | `embedding-3` 建 764/817 页索引 | **无**——查询期 embedding 额度耗尽，**向量路全程未生效** | 53 页建不上索引（25 词条同名 stem 冲突） |

**四条值得记住的判断**：

1. **配置「开了」不等于「生效」——必须端到端验证。** 这是本轮最贵的一课。
   向量检索的 `enabled=true`、索引 764/817 都"成"了，看起来一切正常；
   但查询期 embedding 调用 **429（`code 1113 余额不足或无可用资源包`）**，应用**静默降级**，
   `/search` 的 `vectorHits` 恒为 0、评测里 **0 个向量召回事件**。
   我一度据此写下了「开向量检索修好了 C3/O4」——**是错的**。
   沉淀：新增 `python scripts/vector_index.py probe`（接口自检 + 检索自检两道），
   **改完 embedding 相关配置先 probe，再谈效果**。
2. **归因要靠单变量对照，不能靠时间先后。** 「先改 A、之后变好」不等于「A 导致变好」。
   用两次只差一个变量的运行（v0.3→run2 只改 topK；run2→v1.0 只改模型）才把三件事拆开：
   **C3/O4 ← 检索深度 + 模型；延迟 ← 模型；向量 ← 零贡献。**
3. **"关思考链能提速"是错的。** `glm-4.7 关思考` 的 P50 仍是 **59.5s**（vs 60.1s），
   说明慢的是模型本身；调试时先证伪最容易想到的那个开关，省下无效尝试。
4. **"修好 A 却搞坏 B"是常态，必须全量复测。** 提高检索深度修好了 C3/O4，
   却**把 P1 从"答偏"弄成"拒答"**——只盯目标题就会漏掉这个回归。

> **为什么没有继续在向量上投入**：查询期接口调不动（额度问题不在本项目可控范围），
> 且那 53 页词条仍能被关键词+图谱召回；根因在应用侧以**文件名 stem 作向量页 ID** 的实现约束。
> **判断"什么时候继续"和"什么时候停手"，同等重要。**


---

## 5. 踩坑与学费

| # | 坑 | 代价 | 沉淀 |
|---|---|---|---|
| 1 | 重导后失效引用**卡死编译 worker** | 空转 **6 小时** | `reingest.py` 把「清空引用 wiki 路径的状态文件」写成固定动作 |
| 2 | 中断后无法续跑，只能全量重来 | 重复消耗 | `resume_ingest.py`（file-snapshot 手术法：摘除已编译 + 清队列 → 重扫） |
| 3 | **脱敏是持续动作**：文档/代码注释在「举例说明脱敏规则」处会把真实词写回去 | 反复回流真实公司名/客户名/合同号（**本次新增本报告时又回流 1 次**，见下） | 每次提交前跑**工作区全量**敏感词扫描，且必须用**词边界**匹配（否则短词会命中长词的一部分，如 `ABC` 命中 `ABCDEF`） |
| 4 | 拒答判定关键词写窄了（只有「未找到」） | 2 道正确拒答**被误判成硬答** | 模型表达「不知道」的方式比想象中丰富，判定表要持续补 |
| 5 | 「语料外」题库未先验证 | 测出来的是**检索能力**不是**拒答能力** | 建 `check_refusal_bank.py`：关键词在 119 份语料里命中必须为 0 才能入选 |
| 6 | 会话内启动的后台进程**活不久** | 机器人无法在会话内常驻 | 长驻服务（飞书机器人）只能由用户在外部终端启动 |
| 7 | 本机 Bash 环境损坏（`cd`/`ls`/`dirname` 失效） | 脚本化操作走弯路 | 一律改用 Python 解释器执行 |
| 8 | **提高检索深度后检索面变宽**，把原本能答的那道题的上下文**稀释**掉 | P1 由「答偏」**退化为「拒答」**（一度误以为是"开向量把 P1 搞坏了"） | 认识到「**召回更多 ≠ 更好**」的查全/查准 trade-off；**改检索参数必须全量复测，不能只看目标题** |
| 9 | **应用以「文件名 stem」作向量页 ID**，编译出的同名 concepts/X.md 与 entities/X.md 互相冲突 | 53 页（25 词条）向量索引**整体建不上** | 记录为应用侧约束，评估影响后**主动停手**（关键词+图谱仍可召回，边际收益低） |
| 10 | **检索面变宽会把「编译期派生的真实实体页」带进产物**：`references` 不再只返回关键词命中的页，还会带回 OCR 派生出的真实订单页、配送中心页、第三方物流商页，于是写进了入库的 `eval/评测结果_raw*.json` | 入库产物里出现真实订单号与公司名（v0.3/run2 产物是干净的） | 新增提交前闸门 `scripts/sanitize_eval_results.py`（`--check` 可挂 CI），擦除词表放 gitignored 的 `maps_local.EVAL_SCRUB`；**经验：改检索策略会改变"什么信息会出现在产物里"，脱敏闸门要跟着扩** |
| 11 | **LLM 会「推断」出输入里没有的真实信息**：`data/clean` 已把地址脱敏为 `[地址]`，但模型凭银行地址里的**城市线索**用**世界知识**补出了"所在国家：某南亚国家"。这类残留**任何词表都挡不住** | wiki 内有 58 处派生残留（真实订单号 / 海外地名 / 客户名），占 11 个文件 | 只能"入库后巡检发现 + 人工决定是否回改"；已确认这些残留**只存在于 gitignored 的 `projects/`**，公开仓库不受影响。**取用 wiki 内容写对外文档时必须人工过一遍** |
| 12 | **反扫时用子串匹配，把长单词内部的子串当成命中** | 一度误判"`data/clean` 有残留"，差点去改已经正确的脱敏规则 | 坚持**词边界**匹配（`(?<![A-Za-z0-9])词(?![A-Za-z0-9])`）——这条是第二轮踩过的坑，第三次仍会踩，**必须写进脚本默认行为而不是靠记性** |
| 14 | **拿单轮评测数字当结论**：同一套配置连跑两轮都 18/21，看着很稳，但**两轮未命中的题目几乎不重合**（r1=D1/D2/P1，r2=D1/P3/O2） | 差点把「90.5%」当成可对外承诺的稳定指标；真实情况是 **18~19/21 随机波动**，方差来自框架失败而非知识缺口 | **命中率必须报「区间 + 复跑次数」**，并单独统计「哪些题稳定命中、哪些在摇摆」。改任何配置后至少跑 2 轮全量 + 对摇摆题做多次采样 |
| 16 | **把「最近动过的旋钮」当成原因**（锤子偏差）：D1 命中率只有 1/10，我第一反应是「换 `glm-4.5-air` 的代价」——因为模型是我最近动过的东西。补测后两个假设都被排除：**检索面**（D1 各轮引用数恒为 10 条，与 topK、与 FAQ 页均无关）、**模型**（**从未做过模型对照**，v1.0 的 D1 只有 1 次采样） | 差点把一个「样本量不足」的问题包装成「模型选型」问题写进简历——**那恰好是本项目的反面教材**（见 §4.5 自我纠错）。而且我刚在 §5 坑 13 立过「归因要单变量对照」的规矩，转头就犯 | ① **没有前后各 N 次采样，就不能用「退化」「变差」这类因果词**，只能说「此前未测过基线」；② 遇到异常先问「**这个变量我做过对照吗**」，而不是「我最近改了什么」；③ 写结论前把候选假设连同**排除证据**一起列出来 |
| 17 | **盯着「我能调的旋钮」找解，却没去读那个被我判定为"不可配"的黑盒**：8 步上限被我当成无解硬限制，于是翻来覆去提「换回 glm-4.7 做对照」——**一直在拿模型这个变量做文章**。直到去读应用二进制里嵌的 agent 提示词，才发现失败文案自带 `ask it to continue from the latest result`，而**响应里的 `sessionId` 一直在我的数据里**（`events[0]` 就有 `session_id` 字段，我 dump 过却没看见） | 最讽刺的代价：**答案在我已有的响应里躺了很久**。而「换模型」这个方向即便做成了也要在**全部**题上多付 3.4× 延迟；续跑只在那 10~20% 撞上限的题上多花 ~23s | ① 遇到「框架限制」，**先去读它的报错文案、提示词、响应字段**，再下"无解"的结论；② 报错信息里的**建议动作**（continue / narrow the request）往往是作者留的逃生口；③ 每次归因前先问：**这个变量是我能控的，还是只是我习惯调的？** |
| 18 | **用「测不准」结案**：看到 D1 跨时段 1/10 vs 3/5，我写下「这个系统的噪声大于我调过的任何旋钮」，然后就停了 | 这条结论**方向是错的**——它让人放弃测量。扩样本后（140 次同配置采样）真相是：**只有 4 道题贴边，其余 18 题 90 次采样零失败**；D1 真实作答率 ≈47%，5 次采样测出 20%/60% 只是二项波动。**「方差大」不是结论，是「还没找到方差来自哪」的症状** | ① 遇到「结果不稳定」，**不要停在方差本身**，要接着问「**方差集中在哪些样本上**」——把失败按题分组，一眼就能看出是全局还是局部；② 小样本测出的极端值，先算一下「真实概率 p 下观测到它的概率」再决定要不要惊讶；③ 区分**「我测不准」**与**「这题天生贴边」**——前者无解，后者有明确动作 |
| 15 | **框架异常时模型会把检索原文原样吐出来**（答案以 `I found the following relevant project context` 开头 + 片段原文，无总结段） | ① 被现有判定脚本误计为「有答案」；② **答案正文直接夹带语料原文**——脱敏闸门原本只盯着 `references`，答案正文是盲区 | ① `eval_chat.py` 加「原文直吐」检测判失败（待办）；② 输出侧脱敏必须从「擦引用」扩到「**擦答案正文**」，且要意识到**未知词条（OCR 派生）任何词表都拦不住** |
| 19 | ⭐ **三道脱敏闸门「共用同一张词表」→ 盲区也共用**（2026-09-13）：入库反扫、产物擦除、题集护栏其实都在跑，但词表只收录**已经撞见过一次的**形态。一个**从未进过词表**的真实客户名（出现在订单表的**客户列**，同列其它单元格早已是 `客户F` 这类占位符）就这样通过三道门，**随公开提交写进了 3 个入库文件** | 真实客户名**已经推到 GitHub**；发现时距那次提交已过去一段时间。事后统计：评测产物 **72 处 / 6 文件**需要擦除 | ① **换判据而不是加词**（加词还是打地鼠）：题集护栏从「标题含连续 ≥5 位数字」升级为**「纯拉丁标题（一个中文都没有）一律排除」**，本机再挂完整真实词表；② 产物擦除升级为**「手工清单 + 完整真实词表」两层**，ASCII 词走词边界，防短词命中长词内部（如 `ABC` 命中 `ABCDEF`）；③ **`audit_leaks.py` 不要只跑 `--only clean`**——wiki 层才是应用真正对外检索的那一层。**元教训：当你的检测器和被检测对象共用同一份知识时，它的盲区就是你的盲区。** |
| 13 | **把「配置开了」当成「功能生效了」**：`embeddingConfig.enabled=true` + 索引 764/817 页建成，就据此把 C3/O4 的修复归因于向量 | 一份**已经推送的评测报告归因错误**，整套文档（复盘/质量门槛/简历/README）连带写错；靠复核才发现 | ①新增 `vector_index.py probe`（接口自检 + 检索自检），**端到端验证才算生效**；②**归因必须单变量对照**，不靠时间先后；③报告里凡"某改动带来某效果"都要能指出**对照实验** |
| 20 | ⭐ **「指标变差」时先去怀疑指标，而不是系统**（2026-09-13，P0 处理臂）：注入对照页后 D2 的关键词命中从 **9/10 掉到 3/10**，第一眼就是「注入把 D2 搞坏了」。查原文才发现是**判定误报**——7/10 条答案写的是「『待核单』的『待』**不是指**『等待审核』」，**这恰恰是标准答案里主动纠错的那一句**，而 `NEG_TERMS` 当时只做**子串包含**，看不懂否定，把「越答越对」判成了「越答越错」 | 差一点就对外报出**完全相反的结论**（「对照页有害」），并据此把两页回滚 | ① 判定加**否定语境识别**（`NEG_CUES` + 往回看 14 字符），修完 D2 恢复 10/10、历史 17/140 等数字**逐字不变**；② **指标异常时先抽查 2~3 条原始样本**再下结论——指标是压缩过的信息，压缩就会丢语境；③ 关键词口径天然分不清「提了但说反」，只能当**辅助指标** |
| 21 | ⭐ **把「结果变量」当成「原因变量」→ 得出同义反复的结论**（2026-09-13）：按 `usage.toolEventCount` 分桶得到「24~28 桶失败率 93%」，据此推出「D1/D2 天生需要 ~27 步，所以要减步数」。但 `limit` 失败的**定义**就是「把 8 步预算用满」→ 事件数**必然**≈27 → **必然**落进那个桶 | 整个 P0 的方向（「把 D1/D2 的步数从 18 压到 10」）建立在这个循环论证上；**干净基线一跑就塌**：框架失败 0/20、步数中位 10（2~17），根本没有可改善空间。同一道题的步数跨轮次从 **2 摆到 27** | ① 分桶前先问：**这个变量是被操纵的自变量，还是「结果」的另一种写法？** 若是后者，分桶只是把同一句话换个说法；② **给题目「定性」前必须多轮采样**——单轮/单条件测出的步数不是题的属性；③ 已把读法警告写进 `judge_stability.py` 的输出，防止以后再循环论证 |

---

## 6. 用户价值度量（阶段 7 的核心待办）

### 6.1 现状：当前**没有**真实用量数据

机器人已在真实群聊可用，但**此前没有任何埋点**，因此「找资料耗时下降多少」「服务了多少人」
这两个指标**目前无数据支撑**。

> **本报告不编造这两个数字。** 计划书 §二 的「节省时长」列为「待测量」，此处如实维持「待实测」。

### 6.2 已落地：用量埋点 + 统计脚本（本次新增）

为让这两个指标**可被真实测量**，本次补上了采集链路：

- `scripts/feishu_bot.py` 新增用量埋点：每次问答追加一行 JSON 到 `data/usage_log.jsonl`
  （`data/` 已被 `.gitignore` 隔离，不入公开仓库）。
- **只记元数据，不记问题原文**——业务提问可能自带客户名/订单号等敏感信息，
  与项目「脱敏后才落盘」的口径保持一致；问题与会话均以**单向哈希**记录，可统计去重与频次，无法还原内容。
- 记录字段：时间戳、会话哈希、问题哈希与长度、**耗时**、**引用来源数**、
  是否**多轮追问**、是否**拒答标记**、成功/失败。
- `scripts/usage_stats.py` 一键出数：
  ```
  python scripts/usage_stats.py            # 飞书群聊真实用量
  python scripts/usage_stats.py --md       # 额外输出 Markdown 表（贴回本报告用）
  ```
  输出：总提问数 / 成功率 / **去重提问数** / **唯一会话数（人数近似）** /
  延迟 P50·P90·均值 / 平均来源数 / 拒答标记率 / 多轮追问占比 / 按天分布。

### 6.3 推荐测量方案（「找资料耗时」对照实验）

| 步骤 | 做法 |
|---|---|
| 1. 选样 | 选 5~10 个**典型业务问题**（覆盖概念/流程/操作），问题不预先给答案 |
| 2. 人工基线 | 让培训期新人用既有方式（翻聊天记录 / 群文档 / 手册）找全答案，记录**平均耗时（分钟）** |
| 3. 机器侧 | 同一批问题问机器人，读埋点里的**延迟字段**（秒） |
| 4. 出数 | 效率提升倍数 = 人工平均耗时 ÷ 机器平均耗时；同时报告**一次答准率**与**是否需追问** |
| 5. 口径 | 说明「人工耗时为基线组小样本（n≤10），非全量统计」；机器延迟见下方更新 |

> ✅ **延迟已改善（v1.1 更新）**：问答模型换 `glm-4.5-air` 后，端到端 P50 由 **61.5s → 18.0s**（↓3.4×）。
> 这让「机器侧耗时」这一栏从"分钟级"进入"十几秒级"，但**仍不是"秒级"**——
> 价值差依然是 **「10~30 分钟且可能找不齐」→「十几秒且每条结论可点回来源页」**，
> 对外表述务必按这个口径，不要因为提速了就开始说"秒级"。

### 6.4 结论

| 指标 | 状态 |
|---|---|
| 找资料耗时下降 | ⏳ **待实测**（测量链路已就绪，见 §6.2/6.3） |
| 服务人数 | ⏳ **待实测**（埋点已在统计唯一会话数） |
| 提问次数 / 延迟分布 / 拒答率 | ✅ **可随时出数**（`usage_stats.py`） |

---

## 7. 遗留问题与下一步

| # | 问题 | 现状 | 措施 | 优先级 |
|---|---|---|---|---|
| 1 | ~~检索偶发零召回~~ | ✅ **已修复**（C3/O4 本轮命中） | 提高检索深度 topK **5→15**（**非向量**，向量未生效） | ✅ 已完成 |
| 2 | ~~延迟（P50 61.5s）~~ | ⚠️ **大幅改善但仍未达标**（P50 **18.0s**，↓3.4×） | 已换 glm-4.5-air；可再补答案缓存 / 流式输出 | ⚠️ 改善·未达标 |
| 3 | ~~**术语不匹配导致 P1 稳定未命中**~~ | ✅ **已修复**：根因是题干「订单审核通过」↔ 语料「CP确认/订单理单完成」，答案页从未进入引用。用引擎自带的 `queries/` 页类型**沉淀术语对照页**（`wiki-patches/queries/` + `apply_faq_patch.py`）→ **单变量 A/B 0/5 → 4/5，Fisher 单侧 p=0.024**，两个无关题（D1/D2）两臂无差异 | 继续沉淀术语对照页，**每补一页都要 A/B**，避免过拟合到评测集 | ✅ 已完成 |
| 4 | **框架 8 步工具迭代上限**（**不是"扩散"，是此前没测出来**） | ✅ **v1.3 已定位到具体题目与成因**：同配置 **140 次采样**下失败 **17/140 = 12.1%**（95% Wilson [7.7%, 18.6%]），且**集中在 4 道题**——D1 8/15、D2 6/15、P3 1/5、P1 2/15；**其余 18 题 0/90**（上界 3.3%）。`toolEventCount` 分桶：≤24 → 失败率 0~13%；**24~28 → 93%**（8 步 × ≈3.4 事件/步 ≈ 27）。「与换 glm-4.5-air 相关」是未证实归因，**已撤回** | ① **减少这 4 道题需要的步数**：把对比题的两个概念合并成一页 `queries/` 对照页（P1 用此法失败率已降到 13%）；② session 续跑（v1.2 已实现，**A/B 未达显著 + 会把「无答案」换成「错答案」**，故未推给用户） | **P0** |
| 5 | **向量检索未生效 + 覆盖缺口** | 配置与 764/817 页索引均已就绪，但**查询期 embedding 额度耗尽** → 向量路全程未用（已把 `enabled` 置回 `false` 以匹配现实）；另有 53 页 / 25 词条未建成 | **充值后 `python scripts/vector_index.py enable` 一步恢复（无需重建）**；改页名以绕开 stem 冲突 | P2 |
| 6 | 语料口径冲突 | P2 不计分 | 人工向业务定标 → 修正 wiki 页与评测集 | P2 |
| 7 | 样本量偏小 | 21 计分题 / 8 拒答题 | 扩到 **≥60 题**（支撑"<5%"）、拒答扩到 **≥29 题**（支撑">90%"） | P2 |
| 8 | 机器人防刷 | 无限流 | 限流 / 白名单 | P2 |
| 9 | 引用不可点 | 卡片只列来源路径 | 卡片加「查看来源页」跳转 | P2 |
| 10 | **用户价值实测量** | 待实测 | 按 §6.3 方案执行 | P1 |
| 11 | ⚠️ **词表外的真实专名仍留在 `data/clean` 与 wiki 里**（2026-09-13 发现，见 §5 坑 19） | 入库文件已止血（评测产物**两批共 144 处擦除后 0 残留**、题集护栏已升级）；但**语料侧与 wiki 侧还在**（`audit_leaks.py` 全量：wiki 58 处 / 11 文件）。只在 **gitignored** 目录 → 不影响公开仓库，但**飞书机器人直接答给终端用户时没有擦除层** | **补词表 → 重抽 `data/clean` → 重编 wiki / 重导**。⚠️ 这一步会让前几轮**所有评测数字作废**，必须与评测解耦、单独做一轮；另建议给机器人加一层**出站擦除** | **P0（新增）** |
| 12 | ⚠️ **`git log` 历史里仍有真实专名**（2026-09-13 发现） | 本仓库当前 **private**（个人主页写着「暂未开源」）→ **尚未公开暴露**；但 `2617e0c` **之前**的提交把含真实人名/单号/客户名的旧版推上去过，**只擦 HEAD 不改历史，历史就还在** | **开源前必做**：`git filter-repo`（或删库重建）清历史 → 清完重跑闸门③。**在清历史前不要把仓库转 public**。另：对外宣传语「脱敏零残留」只对 `data/clean` 成立，建议改成「清洗后语料零残留」 | **P0（开源前必做）** |
| 13 | ⚠️ **原「框架 8 步上限 / D1-D2 天生贴边」被自己的干净基线推翻**（2026-09-13，详见 `eval/评测报告_v1.3` §8） | 干净基线 `p0_base`（D1/D2 × 10、`--no-continue`）：**框架失败 0/20**、步数中位 **10**（2~17）。同一道题的步数跨轮次 **2 → 27** ⇒ **步数是「当次运行条件」的函数，不是题的属性**；「24~28 桶失败率 93%」是**同义反复**（limit 失败=用满预算） | 对外**不要再讲「D1/D2 天生贴边」**；主指标从 `framework_failure` 改为 **「零召回率 / 零召回仍作答率」**（`p0_step_reduction.py` 主指标③已实现）——`framework_failure` 抓不到「零召回后改用参数记忆作答」这种**自信的错答案** | **P0（口径已改）** |

> 两项原 P0（检索零召回、延迟）**已落地**：前者靠提高检索深度修复，后者从 61.5s 压到 18.0s。
> 当前**唯一的技术瓶颈收敛为「P1 检索排序」这一道长尾题**（答案在库、仅召回页面错位）。
> 「开向量检索」这条**未交付**——不是没做，是**做了但验证出它没生效**，已如实记录并留了一键恢复入口。


---

## 8. 方法论沉淀（可迁移到其他项目）

1. **先排除工具链问题，再谈模型优化。** 指标不达标时，第一反应应是「是不是我的测试/管线错了」。
   本项目 4.7pp 的提升全部来自修评测脚本，而非调模型或加数据。
2. **「0 缺陷 / 100%」类指标必须带样本量与置信界。** 0/N 的 95% 上界 = `1-0.05^(1/N)`；
   N=21 → 13.3%，要到「<5%」需 N≥60。单写百分比在评审/面试中必然被击穿。
3. **评估盲区要用「一正一反」对照补齐。** 想证明「不会就拒答」，必须同时有
   「语料外应拒答」与「语料内应作答」两组，否则无法区分**真拒答**与**摆烂式拒答**。
4. **脱敏是持续动作，不是一次性任务。** 只要有人改文档就可能把真实词写回注释里的"举例说明"；
   每次提交前必须**全量 + 词边界**反扫。
5. **能复算的数字才是资产。** 每个结论都对应一个可执行脚本（`corpus_stats.py` /
   `check_coverage.py` / `audit_leaks.py` / `eval_chat.py` / `usage_stats.py`），
   面试被追问时能立刻说出「哪个脚本跑出来的」，比数字本身更有说服力。
6. **判断的「转移」本身是结论。** 把瓶颈从「语料覆盖」修正为「检索稳定性」，
   直接避免了继续堆数据的无效投入——停止做错事，比多做对事更省成本。
7. **改检索参数要全量复测，不能只看目标题。** 提高检索深度本是为了修 C3/O4，
   却同时**把 P1 从"答偏"弄成"拒答"**（检索面变宽、上下文被稀释）。只盯着目标题会漏掉回归，
   全量复测才抓到「修好 A 搞坏 B」。**召回更多 ≠ 更好。**
8. **先证伪最容易想到的开关，再换方案。** 「关掉推理模型的思考链」看似是延迟的解，
   实测 P50 只从 60.1s 降到 59.5s——**先花一次测量排除掉这个直觉，才轮到换模型**。
9. **新问题要如实记账，不挑对自己有利的数字。** D2 复跑 2/3 命中（若按多数计命中率 95.2%），
   但主口径仍报单轮实测 **90.5%**；53 页索引失败也写进报告。**敢认局限比数字漂亮更可信。**
10. **改了检索策略，就要重估"什么信息会出现在产物里"。** 检索面变宽后，
    `references` 开始带回**编译期派生的真实实体页**（真实订单号 / 配送中心 / 第三方公司），
    从而写进了入库的评测产物。**脱敏闸门必须跟着数据流向走**——不只是扫输入（语料），
    还要扫输出（评测产物、日志、报告）。
11. **配置「开了」不等于「生效」——必须端到端验证。** 见 §5 坑 13：向量配置与索引全"成"，
    但查询期接口 429、检索里一个向量事件都没有。**判断一个开关是否起作用，
    要看它在下游的数据痕迹（这里是 `vectorHits` / 召回事件），而不是看它自己的状态值。**
12. **归因要靠单变量对照，不能靠时间先后。** 「先改 A、之后变好」≠「A 导致变好」。
    本项目正是靠两次只差一个变量的运行，才把 C3/O4 的功劳从"向量"更正到"检索深度 + 模型"。
    **凡写"某改动带来某效果"，都要能指出那组对照实验。**



---

## 9. 附录：数字来源索引

| 数字 | 来源 / 复算方式 |
|---|---|
| 源素材 173 份 / 153MB | `docs/数据盘点表.md` |
| 语料 121 份 / 1,052,184 字符 | `python scripts/corpus_stats.py`（本次复核） |
| 脱敏 4,667 处（1,586 + 3,081） | `python scripts/corpus_stats.py` |
| 输入侧零残留（`data/clean` 121 份） | `python scripts/audit_leaks.py --only clean`（**退出码 0**） |
| wiki 派生残留 58 处 / 11 文件（**gitignored，不入公开仓库**） | `python scripts/audit_leaks.py --only wiki`（退出码 1；已评估为 LLM 世界知识推断，无法用词表拦） |
| 输出侧零残留（评测产物） | `python scripts/sanitize_eval_results.py --check`（退出码 0） |
| OCR 633 张 | `python scripts/check_coverage.py` |
| 编译 119/119、Wiki 819 页 | `python scripts/wiki_status.py`；819 = 概念 353 + 实体 243 + 来源 119 + 发现 74 + 查询 14 + 方法论 11 + 综合 3 + 索引/日志 2 |
| 命中 19/21 = 90.5%、幻觉 0/21、逐题判定 | `eval/评测结果_raw.json`（2026-09-12，**topK15 + glm-4.5-air，向量未生效**）；分析见 `eval/评测报告_v1.0_检索深度与快模型.md` |
| 引用可溯源 100%（22/22）、延迟 P50 18.0s / 均值 22.1s / P90 31.6s | `eval/评测结果_raw.json` 复算 |
| D2 偶发 2/3、P1 稳定 0/3 | `eval/评测结果_raw_stab_D2P1.json` |
| 检索深度 A/B（C3/O4 双中、P1 双失） | `eval/评测结果_raw_ab_topk10.json`、`_ab_topk15.json` |
| 模型对照 P50 12.9s（glm-4.5-air）/ 60.1s（glm-4.7）/ 59.5s（glm-4.7 关思考） | `eval/延迟基准_20260912_205148.json`（`scripts/bench_chat_latency.py`） |
| **向量检索未生效**（`vectorHits=0`、评测 0 个向量事件、接口 1113） | `python scripts/vector_index.py probe`、`评测结果_raw*.json` 复扫 |
| 向量索引 764/817、失败 53（51 冲突 + 2 非法字符）、25 词条无覆盖 | `scripts/vector_index.py status`、本次复算 |
| 上轮 18/21 = 85.7%、幻觉 0/21、溯源 90.9%、延迟 61.5s | `eval/评测报告_v0.3_复测.md`、`eval/评测结果_raw_v0.3_无向量检索.json` |
| 幻觉 0/21、上界 13.3% | 0 事件 Clopper–Pearson 单侧上界 `1-0.05^(1/21)` |
| 命中率 95% Wilson 区间 71.1~97.3% | n=21, k=19, z=1.96 |
| 拒答 8/8（下界 68.8%）、误拒 0/5 | `eval/拒答评测报告.md`、`eval/拒答评测结果_raw.json` |
| 模型耗时 glm-4.7 66.6s vs glm-4.5-air 7.1s（编译层） | `python scripts/bench_models.py` |
| 脚本 33 个 / 5,683 行 | 本次复核（`scripts/*.py`，排除 `_*.py` 探针与私密映射表） |
| 用量指标（提问数/人数/延迟） | `python scripts/usage_stats.py`（**待真实数据累积**） |
