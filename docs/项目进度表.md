# 项目进度表

> 项目：智能问答助手 ｜ 更新：2026-09-11 ｜ 当前阶段：阶段 2（语料扩容至 119 源，全量重编译中）+ 阶段 5（飞书机器人已跑通）

## 总览

| 阶段 | 名称 | 状态 | 关键产出 |
|---|---|---|---|
| 0 | 需求分析与数据盘点 | 已完成 | 需求分析.md、数据盘点表 |
| 1 | 本地数据整理与清洗 | 已完成 | data/clean/（**121 文件 / 105.2 万字符**）+ 清洗报告 v0.2 |
| 2 | 知识库搭建（LLM Wiki） | 进行中 | 知识库 |
| 3 | 检索与工作流（Dify） | 未开始 | Dify 工作流 |
| 4 | Agent 问答能力 | 未开始 | Agent 逻辑 |
| 5 | 飞书/企微机器人 | 进行中 | 飞书机器人**可用**（卡片 / 多轮 / 重试） |
| 6 | 评测与优化 | **已完成** | 22 题评测集 + 报告 v0.3（**命中率 85.7%**、幻觉 0%） |
| 7 | 上线与量化复盘 | 未开始 | 复盘报告 |

## 阶段 0 · 任务清单

- [x] 明确知识领域与用户画像（产品/业务培训）
- [x] 确定 LLM 选型（国产大模型）
- [x] 输出 `docs/需求分析.md`
- [x] 数据盘点：清点本地文件（清单、类型、规模、主题）

## 阶段 1 · 任务清单

- [x] 编写脱敏清洗脚本 `scripts/clean_text.py`
- [x] 清洗纯文字文件（txt/md，40 个）：脱敏 + 元数据标注
- [x] docx 抽文字（24 个，含 13 份培训手册）：零依赖 XML 解析
- [x] 产出 `docs/清洗报告.md`
- [x] 图片 OCR（67 张聊天截图 + 564 张 docx 内嵌图）—— 已完成，43 份 OCR 产物入 `raw/sources`
- [ ] 纯数字订单号人工核对

## 阶段 2 · 任务清单

- [x] 环境核查：git/python/node/磁盘均正常；无需 Rust/protoc（走便携版）
- [x] 确认模型接入：智谱 GLM（OpenAI 兼容，`https://open.bigmodel.cn/api/paas/v4`）
- [x] 产出 `docs/LLM_Wiki运行手册.md`
- [x] 下载 LLM Wiki 便携版 v0.6.11（走 gh-proxy.com 镜像，SHA256 校验通过）
- [x] 解压到 `tools/llm-wiki/` 并启动验证（GUI 已弹出，本地 API 摸清）
- [x] 产出导入批次清单（`docs/导入批次清单.md`）+ `data/import_batches/` 分批目录（64 文件 5 批）
- [x] 配置智谱 API（模型 glm-4.6，实测通过）
- [x] 修复脱敏漏洞：目录/元数据残留真实姓名 → 学员化名（全项目零残留）
- [x] batch1（12 份智能纪要）导入并编译完成 —— 产出 141 页（concepts 53 / entities 36 / sources 12 / findings 27 / methodology 10）
- [x] batch1 问答验收（本地 API 跑 eval 15 题）—— 命中 11 / 答偏 3 / 未覆盖 1 / 幻觉 0；命中率 73.3%（剔除 O3 盲区 78.6%）；详见 `eval/评测报告.md`
- [x] 修复脱敏漏洞（彻底版）：补全 clean_text.py 映射——简称/尊称/去姓称呼 + 新增人物全部补齐；长词优先替换；元数据头与目录名也脱敏；全项目正文+目录双零残留
- [x] 重建 import_batches（`scripts/build_batches.py`）+ 重导 batch1~4（`scripts/reingest.py`：清空旧 wiki/raw、重置快照、复制 46 文件、rescan）—— batch2 加「逐字稿-」前缀、batch3 去重 2 个；**编译进行中**
- [x] 补数据完成：清洗语料扩至 **121 文件**、`raw/sources` 达 **119 份**（含 43 份截图 OCR）
- [x] 脱敏巡检（`scripts/audit_leaks.py`）：`data/clean` 121 文件 **零残留** ✅
- [x] 定位编译慢根因：`app-state.json` 的 `llmConfig.model = glm-4.7`（旗舰推理模型，实测 66.6s/次，9 成输出是思维链）
- [x] 实测模型提速方案（`scripts/bench_models.py`）：glm-4.5-air 仅 7.1s（快 9.4×）；glm-4.6 当前 429 无额度
- [x] 把「入库模型」单独设为 glm-4.5-air（`taskModelRouting.ingestPresetId`），聊天保留 glm-4.7 —— **已生效**（`wiki_status.py` 实测确认）
- [x] 全量重编译完成：**119/119 份已编译**，processing=0 / pending=0 / failed=0，wiki 819 页
- [x] 22 题复测完成（`eval/评测报告_v0.3_复测.md`）：命中率 **85.7%**（18/21）、幻觉 **0%**；修掉评测链路两处工程缺陷（502 无重试、项目 ID 解析 404）后 O3/O5/C1 由失败转命中
- [ ] 消除检索偶发零召回（C3/O4）：向量检索当前关闭（`embeddingConfig.enabled=false`），仅靠关键词+图谱
- [ ] 延迟优化：端到端 P50 61.5s，距质量门槛 <3s 差距大（口径不同，但需标注）
- [ ] 向业务确认 P2 口径（核料纸样 vs 是否需要制版，语料两处矛盾）

## 里程碑

| 日期 | 里程碑 | 备注 |
|---|---|---|
| 09-09 | 项目启动，计划书与进度表建立 | 阶段 0 开始 |
| 09-09 | 需求分析完成，确认领域/数据/选型 | 数据已下载到本地 |
| 09-09 | 数据盘点完成（173 文件 / 153MB） | 产出数据盘点表 |
| 09-09 | 纯文字清洗完成（40 文件，脱敏 1357 处） | 产出清洗报告 |
| 09-09 | docx 抽文字完成（24 文件） | 累计清洗 64 文件 |
| 09-09 | 阶段 2 启动：环境核查 + 智谱接入确认 | 产出运行手册；下载卡在 GitHub 网速 |
| 09-09 | LLM Wiki 下载/校验/解压/启动完成 | 走 gh-proxy 镜像，v0.6.11 已跑起来 |
| 09-10 | 智谱 API 配置完成（glm-4.6）+ batch1 进项目开始编译 | 提前构建评测集（15 题） |
| 09-10 | batch1 编译完成（141 页：概念 53/实体 36/规则 27/操作 10/来源 12） | 待验收；发现脱敏残留 |
| 09-10 | batch1 问答验收完成（本地 API 自动跑 15 题） | 命中 11/答偏 3/未覆盖 1/幻觉 0；命中率 73.3%；产出评测报告 |
| 09-11 | 语料扩容：`data/clean` 64→**121 文件**、`raw/sources` **119**（含 43 OCR） | 语料规模翻倍，触发全量重编译 |
| 09-11 | 飞书机器人跑通（截图实测命中评测题 C3） | 阶段 5 骨架 → 可用 |
| 09-11 | 脱敏巡检通过（clean 零残留）；定位编译慢根因 = glm-4.7 | 详见变更日志 |
| 09-12 | **全量编译完成**：119/119 份、0 失败、wiki 819 页；自动评测命中率 81.0%、幻觉 0% | 语料覆盖 100% |
| 09-12 | **复测 v0.3**：修 502 重试 + 项目 ID 解析 → 命中率 **85.7%**（18/21）、幻觉 0%，**首次达成 ≥85% 目标** | O3/O5/C1 由失败转命中 |

## 变更日志

| 日期 | 变更 |
|---|---|
| 09-09 | 初始化项目计划书与进度表 |
| 09-09 | 完成需求分析；确认产品/业务培训领域、数据已本地化、国产 LLM |
| 09-09 | 完成数据盘点；产出 docs/数据盘点表.md |
| 09-09 | 架构澄清：RAG 包含 LLM Wiki；function calling 用于意图路由；MCP 列为可选扩展 |
| 09-09 | 纯文字清洗完成（脱敏 1357 处）；产出 docs/清洗报告.md 与 data/clean/ |
| 09-09 | docx 抽文字完成（24 文件）；累计清洗 64 文件、脱敏约 1512 处 |
| 09-09 | 阶段 1 收尾，进入阶段 2；确认 LLM Wiki 走便携版、模型定智谱 GLM；产出运行手册 |
| 09-09 | LLM Wiki v0.6.11 下载（gh-proxy.com 镜像）、SHA256 校验、解压、启动验证完成 |
| 09-09 | 产出导入批次清单（docs/导入批次清单.md）；建 data/import_batches/ 把 64 份语料按主题切成 5 批 |
| 09-10 | 智谱 API 配好（glm-4.6 实测通过）；修复真实姓名脱敏漏洞（目录名+元数据，全项目零残留）；batch1 复制进项目并开始编译 |
| 09-10 | 摸清 LLM Wiki 本地 HTTP API（19828，v1 端点含 search/chat），用 API token 打通机器评测；写 `scripts/eval_chat.py` 自动跑 15 题 |
| 09-10 | batch1 验收：命中 11/答偏 3/未覆盖 1/幻觉 0；命中率 73.3%（剔除 O3 盲区 78.6%）；产出 `eval/评测报告.md`；发现 O3 翻单枚举在 batch3、P2 语料口径矛盾、脱敏残留（简称/尊称未覆盖） |
| 09-10 | 等待编译期间提前产出 `eval/评测集.md`（15 题，覆盖概念/对比/流程/操作四类，带标准答案要点） |
| 09-11 | 完成机器人接入调研：飞书为主（个人自建应用 + 长连接，免公网）、企微受限（仅内部群）；对接走 LLM Wiki 本地 HTTP API（复用 eval_chat.py 链路） |
| 09-11 | 落地飞书机器人骨架 `scripts/feishu_bot.py`（实测 lark-oapi 1.7.3 全部 API 名 + 项目解析通过）；发现编译卡死根因：模型被换成 glm-4.7（旗舰推理模型），待切回 glm-4.6 |
| 09-11 | 补数据完成并触发全量重编译（`reingest.py` 16:27）：`raw/sources` 达 119 份（含 43 OCR），wiki 从 2 源重建中 |
| 09-11 | 飞书机器人实测跑通：群内 @ 提问「什么是短溢装」→ 结构化答案 + 来源引用（命中评测题 C3） |
| 09-11 | 脱敏巡检（`audit_leaks.py`）：`data/clean` 121 文件零残留；`projects/` 内 `.llm-wiki/review.json`、`wiki/sources/*.md` 复现派生敏感词（若干客户名与海外地名）——均为 LLM 编译阶段派生，且已被 `.gitignore` 隔离，公开仓库不受影响 |
| 09-11 | 评测集升级 **v0.2**：新增 Q 类（报价/配比，batch2+4）6 题 + M 类（机制）1 题，共 **22 题**；修正 C2 标准答案（短溢装设置）、P2 改为**不计分**（口径待业务确认）；同步 `scripts/eval_chat.py`（新增 `scoring` 标记） |
| 09-11 | 飞书机器人增强 **v0.2**：卡片消息 + 进度卡片**原地更新**（patch）+ **原生多轮会话**（复用 LLM Wiki `/chat` 的 sessionId）+ 超时退避重试 + 内置命令（/help、/reset）+ `--selftest`/`--ask` 自检；`config.load_bot_settings()` 支持 env 调参 |
| 09-11 | 新增只读统计脚本 `scripts/corpus_stats.py`，复算语料与脱敏数字：**121 文件 / 105.2 万字符**；脱敏命中 **4,667 处**（文本 1,586 + 截图 OCR 3,081） |
| 09-11 | 文档收尾：`docs/简历素材.md` 升 v0.2（回填全部可复算数字）、`docs/清洗报告.md` 升 v0.2（对齐 121 份语料 + 7 类脱敏规则 + 巡检结果）、`README.md`（补 OCR/机器人/统计脚本、22 题、技术栈） |
| 09-11 | **分模型落地**：入库/编译模型独立配 `glm-4.5-air`（`scripts/set_ingest_model.py`），问答**跟随界面当前预设不锁定**；`scripts/.env` 的 key 同步为应用在用新 key |
| 09-11 | 中断后续跑固化为 `scripts/resume_ingest.py`（`--plan`/`--apply`/`--rescan`，file-snapshot 手术法：摘除未编译条目 + 清队列 → 重扫），已执行：已编译 16 份保留、待续跑 103 份重建 |
| 09-11 | 新增 `docs/质量门槛对标.md`：企业级 RAG 评估维度（准确率/忠实度/拒答/溯源/覆盖度）+ 参考门槛 + 基线逐题归因 + 差距清单，澄清「73.3% 是 19% 覆盖下的中间快照」；`eval/评测报告.md`、`README.md` 加交叉引用 |
| 09-12 | 全量编译完成（119/119，0 失败，819 页）；`monitor_compile.py` 自动触发 22 题评测 → **81.0%**（17/21）、幻觉 0%；O3/O5 因 HTTP 502 丢分、C1 零召回 |
| 09-12 | 评测链路工程修复：`eval_chat.py` ① 新增 `resolve_project_id()`（`/projects/current` 在 currentProject 为 null 时 404，改为从 `/projects` 取 ID）；② 对 429/5xx/超时加指数退避重试（3 次）；③ 从 `events` 兜底提取 `event_refs` 以区分「检索到未采用」与「真零召回」 |
| 09-12 | **复测 v0.3**：命中率 **85.7%**（18/21）、答偏 1（P1）、零召回 2（C3/O4）、**网关失败 0**、幻觉 **0%**；达成计划书 ≥85% 目标。新产品 `eval/评测报告_v0.3_复测.md` |
| 09-12 | 新增覆盖度检查脚本 `scripts/check_coverage.py`：按元数据「原文件名」反向追溯（脱敏会改名），单独统计图片 OCR / PDF / XLSX；确认有效业务知识已全量入库，未导入的均为**有意排除的客户原始订单 PDF** 与内容已覆盖的重复件 |
